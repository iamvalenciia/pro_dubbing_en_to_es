# === BINDING ESTRICTO DE AUDIO Y NUCLEO MATEMATICO ===
# Crítico: numpy 2.0 rompe la ABI C de librosa, soundfile y numba.
numpy<2.0.0
scipy==1.13.1

# === INTEGRACION LLM & PIPELINE ===
# Utilizado por phase7_ai_shorts.py para generar plan de Shorts con Gemini.
google-generativeai==0.8.2
google-genai

# === UI / SERVIDOR ===
gradio>=5.0.0
fastapi>=0.115.0
uvicorn>=0.32.0

# === VIDEO ===
ffmpeg-python==0.2.0
librosa==0.10.2.post1
soundfile==0.12.1
faster-whisper==1.1.1

# === UTILIDADES ===
python-dotenv
tqdm
gdown
addict==2.4.0
requests
pydub
Pillow>=10.0.0
tenacity==9.1.2
ten-vad>=1.0.6.8
aiohttp==3.12.13
openai
deepgram-sdk==4.1.0
elevenlabs
azure-cognitiveservices-speech
camb-sdk
dashscope==1.25.5
transformers==4.57.3
edge-tts
gTTS==2.5.1
piper-tts>=1.3.0
zhconv==1.4.3

# === DIARIZACIÓN CLUSTERING ===
# Requerido por ali_CAM Phase 1: clustering HDBSCAN de embeddings de voz.
# Sin estos, el pipeline de diarización falla con ImportError en tiempo de ejecución.
hdbscan
umap-learn

# === MODELSCOPE / FUNASR (diarización ONNX) ===
# Motor ASR y pipeline de diarización CAM++.
# datasets + simplejson + sortedcontainers requeridos por funasr pipeline internamente.
modelscope>=1.34.0
funasr>=1.3.1
datasets
simplejson
sortedcontainers

# === ALIGNER ONNX ===
# sherpa-onnx: requerido por Qwen3-ForcedAligner para alineación fonética.
sherpa-onnx>=1.12.15
