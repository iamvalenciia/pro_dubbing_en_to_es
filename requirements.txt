# === BINDING ESTRICTO DE AUDIO Y NUCLEO MATEMATICO ===
# Crítico: numpy 2.0 rompe la ABI C de librosa, soundfile y numba.
numpy<2.0.0
scipy==1.13.1

# === MANIPULACION DE AUDIO (Fase 2 y 5) ===
# NOTA: demucs y pyannote.audio removidos — la fase 1 (separacion voz/ambiente)
# fue eliminada del pipeline. pyrubberband tambien removido (no hay time-stretch).
librosa==0.10.2
soundfile==0.12.1
pydub==0.25.1

# === HUGGINGFACE & TRANSFORMERS ===
# transformers lo pinean qwen-tts (4.57.3) y qwen-asr (4.57.6) via Dockerfile en pasos secuenciales.
# accelerate lo clava qwen-asr a 1.12.0.
# sentencepiece es requerido por MarianTokenizer (Fase 3).
accelerate==1.12.0
sentencepiece>=0.1.99
# sacremoses: recomendado por MarianTokenizer para tokenizacion EN->ES de calidad.
sacremoses>=0.1.1
# safetensors: requerido para convertir pytorch_model.bin de MarianMT post-download
# (dodge del bloqueo CVE-2025-32434 que transformers aplica con torch < 2.6).
safetensors>=0.4.0

# NOTA: qwen-asr y qwen-tts se instalan en el Dockerfile en pasos separados
# porque tienen pines exclusivos de transformers que pip no puede resolver juntos.

# === INTEGRACION LLM & PIPELINE ===
# google-generativeai removido — Fase 3 ahora usa MarianMT (local, sin API).
# Se mantiene instalable si el modulo phase7_ai_shorts.py lo sigue usando
# (shorts con Gemini). Si no usas shorts, podes sacarlo.
google-generativeai==0.8.2

# === VIDEO & SUBS & UI ===
opencv-python-headless==4.10.0.84
moviepy==1.0.3
ffmpeg-python==0.2.0
pysubs2==1.7.3
# Stack de Gradio fijado para evitar TypeError: 'bool' is not iterable en /info
# (gradio_client parser no maneja additionalProperties: true de pydantic 2.10+)
gradio==4.44.1
gradio_client==1.3.0
pydantic==2.8.2
fastapi==0.112.4
uvicorn==0.30.6

# === UTILIDADES ===
python-dotenv
tqdm
gdown
